{
  "name": "mrtc-calendar",
  "version": "1.0.0",
  "private": true,
  "description": "MRTC Calendar - the centralized event, schedule, responsibility and planning calendar for MRTC.",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "start": "npm run start --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "seed": "npm run seed --workspace server",
    "reset": "npm run reset --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
